# Gazebo bug - need to download the models manually

cd catkin_ws/src

sudo apt-get install mercurial

hg clone https://bitbucket.org/osrf/gazebo_models

export GAZEBO_MODEL_PATH=$GAZEBO_MODEL_PATH:~/catkin_ws/src/gazebo_models
